from .planner import planner
def algorithm(start_index,goal_index,width,height ,costrap, resolution, origin, costmap ,grid_viz ):
    return planner(start_index,goal_index,width,height ,costrap, resolution, origin, costmap ,grid_viz )